{
  "name": "copypaste-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for the CopyPaste Chrome extension and Electron AI Project Builder controller.",
  "workspaces": [
    "apps/desktop",
    "apps/extension",
    "packages/protocol"
  ],
  "scripts": {
    "desktop": "npm --workspace next-step start",
    "desktop:dev": "npm --workspace next-step run dev",
    "desktop:test": "npm --workspace next-step test",
    "extension:check": "npm --workspace @copypaste/extension run check",
    "extension:test": "npm --workspace @copypaste/extension run test",
    "extension:verify": "npm --workspace @copypaste/extension run verify",
    "test": "npm run extension:test && npm run desktop:test",
    "verify": "npm run extension:verify && npm run desktop:test"
  }
}
